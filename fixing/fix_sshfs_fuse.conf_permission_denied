From: https://goo.gl/29BYcx

===
Fix for the problem "fusermount: failed to open /etc/fuse.conf: Permission 
denied" using sshfs

Fix that works for Ubuntu 15.04 vivid
===

If current user is not in fuse group, checking via
$ groups <user>
then
$ sudo addgroup <user> fuse

If problem still occur:
$ sudo chmod a+r fuse.conf

